<%= form_with url:"/quizzes/judge", local: false, method: :get do |f| %>
  <%= f.hidden_field :quiz_id, :value => quiz.id %>
  <%= f.text_field :word, placeholder:"答えを入れてください", class:'text_field' %>
  <%= f.submit "投稿" %>
<% end %>